var assert   = require('assert'),
    solution = require('./solution.js');

describe('01: Multiples of 3 and 5', function() {
	it('Test case 1', function() {
      assert.equal(solution(), 233168);
    });
});